name: Day 44 Task-1 | Github Secrets

on:
    workflow_dispatch:

jobs:
    test-secret:
        runs-on: ubuntu-latest
        steps:
            - name: print secret
              run: echo "Secret is:" ${{secrets.MY_SECRET_MESSAGE}}
